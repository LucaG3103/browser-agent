// API Configuration Template
// RINOMINA questo file in "apiConfig.js" e inserisci la tua chiave REALE
// Questo file può essere committato su GitHub perché NON contiene la chiave.

const API_CONFIG = {
    // Inserisci la tua API key qui
    OPENAI_API_KEY: "INSERISCI_LA_TUA_KEY_QUI",

    // Se true, usa la key hardcoded sopra invece di quella salvata nello storage
    USE_HARDCODED_KEY: true
};

// Non modificare sotto questa linea
